<%= form_for @artist do |f| %>
  <%= f.label :name, 'Enter the artist name' %>
  <%= f.text_field :name %>
  <br><br>
  <%= f.label :bio, 'Enter the artist bio' %>
  <%= f.text_area :bio %>
  <br><br>
  <%= f.submit %>
<% end %>
